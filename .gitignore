# Build outputs
target/
dist/
**/generated-sources/

# Frontend
node_modules/
.vite/
.bun/
frontends/*/dist/

# Built SPA bundles copied into backend
backend/poll-api/src/main/resources/static/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Local env
.env
.env.local

# go-task e2e orchestration marker (tracks whether `task test:e2e:*` started the stack)
.task-e2e-backend-started

# Slidev demo deck pulls slug / pollId / questionIds / deck token from the
# operator's local data.ts. The file is regenerated by hand or by the
# "Copy snippet" backoffice action — kept gitignored so per-developer
# values do not collide.
frontends/slidev-demo/data.ts
.superpowers/

# AI tooling (local-only, not tracked)
.tessl/
.claude/
.codex/
.cursor/
.gemini/
.agents/
.github/skills/
.github/prompts/
specs/
openspec/
AGENTS.md
CLAUDE.md
CONSTITUTION.md
PREMISE.md
tessl.json
