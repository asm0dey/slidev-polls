# Build outputs
target/
dist/
**/generated-sources/

# Frontend
node_modules/
.vite/
.bun/
frontends/*/dist/

# Built SPA bundles copied into backend
backend/poll-api/src/main/resources/static/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Local env
.env
.env.local

# go-task e2e orchestration marker (tracks whether `task test:e2e:*` started the stack)
.task-e2e-backend-started

# Slidev demo seed output - rewritten by `bun run seed` on every demo run
# with a fresh slug / pollId / questionIds / deck token. Run seed before
# `bun run slidev` on a fresh clone.
frontends/slidev-demo/data.ts
.superpowers/
